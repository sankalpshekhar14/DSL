#include "polynomial.h"

void main(){
	poly_pointer p1,p2,sum;
	p1=(poly_pointer)malloc(sizeof(POLY));
	p2=(poly_pointer)malloc(sizeof(POLY));
	sum=(poly_pointer)malloc(sizeof(POLY));

	while(i!=0){
			printf("Enter ")
	}
}